import { MD3LightTheme as DefaultTheme, MD3Theme } from 'react-native-paper'

export const theme: MD3Theme = {
  ...DefaultTheme,
  roundness: 2,
  version: 3,
  colors: {
    ...DefaultTheme.colors
  }
}
